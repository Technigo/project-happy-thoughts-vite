export const Feed = () => {
  return (
    <section className="feedContainer">
      <p>Feed: returning the latest 20 thoughts from API</p>
      <div className="cardContainer">
        <p>Card: returning a single thought from API</p>
        <p>heart</p> <p>count hearts</p>
      </div>
      <div className="cardContainer">
        <p>Card: returning a single thought from API</p>
      </div>
    </section>
  );
};
